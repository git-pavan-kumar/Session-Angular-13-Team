import { CustomerHttpDetails } from './customer-http-details';

describe('CustomerHttpDetails', () => {
  it('should create an instance', () => {
    expect(new CustomerHttpDetails()).toBeTruthy();
  });
});
